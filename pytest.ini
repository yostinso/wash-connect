[pytest]
asyncio_mode = auto
testpaths = tests tests/ha
pythonpath = . custom_components
markers =
    interactive: requires interactive stdin (run with -s and -m interactive)
addopts = -m "not interactive"
